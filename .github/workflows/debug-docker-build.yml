name: Debug Docker Build

on:
  workflow_dispatch:

jobs:
  debug-docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build .
